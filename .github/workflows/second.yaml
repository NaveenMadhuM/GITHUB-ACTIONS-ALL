name: first workflow
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [14, 16,12,8]
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}   
